import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coinleague.dexkit.app',
  appName: 'Coinleague',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
