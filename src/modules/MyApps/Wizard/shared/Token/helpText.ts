import {Token} from 'types/app';
import {HelpText} from '../';

export const HELP_TEXT_TOKEN: HelpText<Token> = {
  address: ['Enter the token address'],
  name: ['Token name'],
  icon: ['Token imagem url'],
  symbol: ['Token symbol'],
  decimals: ['Token decimal representation'],
  chainId: ['ChainId'],
  price_usd: ['Token price in USD'],
};
