import {makeStyles} from '@material-ui/core';

export const useModuleStyle = makeStyles((theme) => ({
  boldText: {
    fontWeight: 600,
  },
  coinSelectImage: {
    height: 'auto',
    width: '100%',
  },
}));
